general:
    common_entity_attributes:
        - name: oldEnt
          type: struct gentity_s*

messages:
    PrepareNetCode:

    FreeAt:
        freeTime: int

    Think:
        timeDelta: int

    Ignite:
        fireStarter: struct gentity_s*

    Extinguish:
        immunityTime: int

components:
    DeferedFreeing:
        messages:
            - FreeAt

    Ignitable:
        parameters:
            alwaysOnFire: bool
        messages:
            - PrepareNetCode
            - Think
            - Ignite
            - Extinguish

entities:
    Empty:
        components:

    Fire:
        components:
            Ignitable:
                alwaysOnFire: "true"
            DeferedFreeing:

    AlienBuildable:
        components:
            Ignitable:
                alwaysOnFire: "false"

# vi:ts=4:et:ai
