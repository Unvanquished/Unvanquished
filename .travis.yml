language:
  - c
  - c++

compiler:
  - clang
  - gcc

install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install cmake

script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build . --target install
